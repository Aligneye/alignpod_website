export type PostureResult = {
  score: number;
  summary: string;
  headPosition: string;
  shoulderAlignment: string;
  upperBack: string;
  spineAwareness: string;
  recommendations: string[];
  disclaimer: string;
};